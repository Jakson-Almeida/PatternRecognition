# Classificação de FBGs relevantes para demodulação de LPFG

Trabalho experimental do Trabalho Final (Reconhecimento de Padrões).

## Estrutura

```
classification/
├── notebooks/     # experimentos (Passo 0, 1, ...)
├── figures/       # figuras geradas
├── results/       # tabelas / artefatos intermediários
├── src/           # utilitários (máscara, I/O)
└── requirements.txt
```

## Dados

Fonte: `../fbg-demodulated-lpfg/data/measured.dataset`

## Setup

```bash
pip install -r requirements.txt
```

Abrir `notebooks/0 - Setup.ipynb` e executar todas as células.
